@page "/poster"
<img src="@currentPic" @onload="@doneLoading" hidden="@hide" />

@code {
    string currentPic = "Pics/LarsonCut.png";
    bool hide = true;
    public void doneLoading()
    {
        var synthesizer = new SpeechSynthesizer();
        synthesizer.SelectVoiceByHints(VoiceGender.Female);
        synthesizer.Speak("PRESENTING in 3...2...1");
        hide = false;
        synthesizer.Speak("Snowpiercer: The Larson Cut");

        var syn = new SpeechSynthesizer();
        syn.SetOutputToDefaultAudioDevice();
        syn.Speak("Hey, I'm only going to write the script though, I'm not a director lol");
    }
}
